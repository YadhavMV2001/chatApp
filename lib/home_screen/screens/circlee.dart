import 'package:flutter/material.dart';

class Circlee extends StatelessWidget {
  const Circlee({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Circlee"),
      ),
    );
  }
}
